<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>DonasiKu</title>

  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.bunny.net">
  <link href="https://fonts.bunny.net/css?family=figtree:400,500,600&display=swap" rel="stylesheet" />

  <!-- Tailwind CSS via CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            sans: ['Figtree', 'sans-serif'],
          },
          colors: {
            primary: {
              DEFAULT: "#1b5e20", // Dark green
              light: "#4c8c4a",
              dark: "#003300",
            },
            secondary: {
              DEFAULT: "#212121", // Black
              light: "#484848",
              dark: "#000000",
            },
          }
        }
      }
    }
  </script>

  @vite(['resources/css/app.css', 'resources/js/app.js'])
  @!section('styles')
</head>
<body class="antialiased font-sans bg-gray-50">
  <div class="min-h-screen flex flex-col">
    <!-- Header -->
    <header class="bg-primary text-white shadow-md">
      <div class="container mx-auto px-4 py-4 flex justify-between items-center">
        <div class="flex items-center">
          <a href="/" class="text-2xl font-bold">DonasiKu</a>
        </div>
        
        <div class="flex space-x-4">
          @if(!auth.isLoggedIn)
            <a href="/login" class="text-white hover:text-green-200">Login</a>
            <a href="/register" class="bg-white text-primary hover:bg-green-100 px-4 py-2 rounded-lg font-semibold">Register</a>
          @else
            <span class="text-white">Welcome, {{ auth.user.fullName }}</span>
            <a href="/dashboard" class="text-white hover:text-green-200">Dashboard</a>
            <form action="/logout" method="POST" class="inline">
              {{ csrfField() }}
              <button type="submit" class="text-white hover:text-green-200">Logout</button>
            </form>
          @endif
        </div>
      </div>
    </header>

    <!-- Content -->
    <main class="flex-grow">
      @!section('content')
    </main>

    <!-- Footer -->
    <footer class="bg-primary text-white py-6">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <h2 class="text-xl font-bold">DonasiKu</h2>
        <p class="mt-2 text-green-200">Platform donasi online terpercaya untuk menyalurkan bantuan kepada mereka yang membutuhkan.</p>
        <p class="mt-4 text-green-200">Email: example@donasiKu.com</p>
      </div>
    </footer>
  </div>

  @!section('scripts')
</body>
</html>
